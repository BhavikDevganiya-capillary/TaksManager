describe("write test cases for task manager", () => {
  test("ui test cases", () => {
    //Check wether navbar loaded or not
    //check wether header is there or not
    //check wether all links are there or not to navigate
    //check wether button is there or not
    //Check if redirection is working when you are click on link or not
  });
  test("Slide Form testcases", () => {
    //Check wether input box is there when page loads
    //check wether add slide button is there or not
    //check if input box is empty then its creating slide or not
    //Enter slide name using fireEvent and check wether its there in input box or not
    //click button using fireEvent check is button is clicked or not
    //check wether new slide is added to the ui or not
    //Check wether slides name is similar as you entered or not
    //check if there is inputbox and button to create task
    //check if inputbox is empty and you click on add task button does it create task or not
  });
});
